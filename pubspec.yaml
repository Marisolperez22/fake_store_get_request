name: fake_store_get_request
description: "Un pacquete que permite hacer una petición a la API fakestore para
  obtener una lista de productos"
version: 0.0.2
homepage: https://github.com/Marisolperez22/fake_store_get_request

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  either_dart: ^1.0.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  http: ^1.4.0
  matcher: ^0.12.17

dev_dependencies:
  build_runner: ^2.6.0
  coverage: ^1.7.0
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.5.0

flutter: null
